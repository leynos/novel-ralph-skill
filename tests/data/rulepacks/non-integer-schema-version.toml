# schema_version = "1" (a TOML string): must raise with rule_id is None; "1" is
# NOT cast to the integer 1.
schema_version = "1"
pack = "ai-isms"

[[rule]]
id = "tapestry"
pattern = "\\btapestry\\b"
threshold = 0
basis = "manuscript"
